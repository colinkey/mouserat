{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://mouserat/schemas/request",
  "title": "Request",
  "description": "An individual HTTP request within a collection",
  "type": "object",
  "required": ["id", "name", "method", "url"],
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string"
    },
    "id": {
      "type": "string",
      "format": "uuid",
      "description": "Unique identifier for the request"
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "description": "Display name for the request"
    },
    "method": {
      "type": "string",
      "enum": ["GET", "POST", "PUT", "PATCH", "DELETE", "HEAD", "OPTIONS"],
      "description": "HTTP method"
    },
    "url": {
      "type": "string",
      "description": "URL path appended to the collection or environment root URL"
    },
    "headers": {
      "type": "object",
      "description": "HTTP headers to include with the request",
      "additionalProperties": {
        "type": "string"
      }
    },
    "body": {
      "description": "Request body. Serialized as JSON unless overridden by a Content-Type header."
    },
    "jqFilter": {
      "type": "string",
      "description": "Default jq filter applied to the response. Can be overridden at runtime."
    }
  }
}
